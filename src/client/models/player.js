export default class Player {

    constructor(username, totalScore, health, level) {
        this.username = username;
        this.totalScore = totalScore;
        this.health = health;
        this.level = level;
    }
}